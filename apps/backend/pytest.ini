[pytest]
# Only collect from tests/. The test_*.py files in this directory are manual
# debug scripts that need live services and run on import.
testpaths = tests
markers =
    live: contract test against a real model provider; skipped unless run with --live
